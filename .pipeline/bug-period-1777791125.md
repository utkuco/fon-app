# Pipeline Scratchpad — Gainers Period Bug Investigation

## Summary (2026-05-03)
Researcher: period dependency array'de mevcut. GainersSection kodu doğru. Buton click çalışıyor.
E30d7ee commit'i get3M() düzeltmesi içeriyor — Supabase DB'de returns["3M"] düzeltildi.

## Key Findings

### PMP 3A Bug — ROOT CAUSE
- Supabase `funds.returns["3M"] = 375.37` bozuk veri (scraper bug)
- Homepage `get3M()` eski kod: `fromJson["3M"]` kontrol ediyordu → +375.37% döndü
- Detail page `get3M()` farklı: `return_3m` kullanıyordu → -1.84% doğru değer
- **FIX**: `returns["3M"]` = null olarak ayarlandı (12 bozuk kayıt düzeltildi)
- **ALSO**: e30d7ee commit get3M() sıralamasını düzeltti (return_3m önce geliyor)

### DB Fix (2026-05-03)
```
psycopg2: UPDATE funds SET returns = jsonb_set(returns, '{3M}', 'null')
WHERE (returns->>'3M')::numeric > 100 OR (returns->>'6M')::numeric > 100;
Affected: 12 rows including PMP
PMP returns["3M"]: 375.37 → null
```

### Still Unresolved: GainersSection 3A Button
- Buton tıklandığında column header değişmiyor gibi görünüyor
- Ancak PMP artık doğru değer gösteriyor (detail page)
- Araştırılmadı — Supabase DB düzeltmesi öncelikliydi

## Remaining Tasks
1. Türk fon scraper cron kontrolü
2. ETF cron kurulumu (Yahoo Finance → Supabase)
3. GainersSection 3A button detaylı test