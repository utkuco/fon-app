# CODER RESULTS: ReturnsTable Refactor

## Task
Add oneWeekReturn/oneMonthReturn/threeMonthReturn/sixMonthReturn props to ReturnsTable, replace getReturn(7/30/90/180), update EtfDetailClient call site.

## Changes Made

### 1. ReturnsTable.tsx
**Path:** `~/Desktop/projects/fon-app/web/src/components/ReturnsTable.tsx`

- Added 4 new props to `ReturnsTableProps` interface:
  - `oneWeekReturn: number | null`
  - `oneMonthReturn: number | null`
  - `threeMonthReturn: number | null`
  - `sixMonthReturn: number | null`

- Updated function signature to destructure new props

- Replaced return rows:
  - `getReturn(7)` → `oneWeekReturn`
  - `getReturn(30)` → `oneMonthReturn`
  - `getReturn(90)` → `threeMonthReturn`
  - `getReturn(180)` → `sixMonthReturn`

- Added new props to `useMemo` dependency array

### 2. EtfDetailClient.tsx (lines 558-561)
**Path:** `~/Desktop/projects/fon-app/web/src/app/etf/EtfDetailClient.tsx`

- Added 4 new props to `<ReturnsTable />` call:
  - `oneWeekReturn={etf.one_week_return_try != null ? etf.one_week_return_try * 100 : null}`
  - `oneMonthReturn={etf.one_month_return_try != null ? etf.one_month_return_try * 100 : null}`
  - `threeMonthReturn={etf.three_month_return_try != null ? etf.three_month_return_try * 100 : null}`
  - `sixMonthReturn={etf.six_month_return_try != null ? etf.six_month_return_try * 100 : null}`

### 3. FundDetailClient.tsx (line 413)
**Path:** `~/Desktop/projects/fon-app/web/src/app/fon/[code]/FundDetailClient.tsx`

- Added `oneWeekReturn={null}`, `oneMonthReturn={null}`, `threeMonthReturn={null}`, `sixMonthReturn={null}` to `<ReturnsTable />` call (pre-funds lack these fields)

## TypeScript Check
Ran `npx tsc --noEmit` — no new errors introduced by this change. Remaining errors (debug-etfs route module, homepage-data.test.ts category_stats, formatters.test.ts) are pre-existing and unrelated.

## Notes
- Pre-computed return values (oneWeekReturn etc.) are already percentages from the database (×100 applied by cron), so displayed directly without additional multiplication
- The `getReturn(1)` (Günlük), ybbReturn, and `getReturn(365)` (1 Yıllık) still computed from priceHistory as before
